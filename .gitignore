node_modules
dist
# SugarDeploy's per-game engine bundle output; ignored by the top-level
# `dist` rule above but the deploy-frontend GHA job needs it committed.
# Stopgap until Plan 048 lands (engine bundle comes from GHCR; this line
# can go away then).
!.sugarmagic/published-web/dist
coverage
.DS_Store
.vite
*.tsbuildinfo
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo

*.rar
