# GENERATED BY SUGARMAGIC - DO NOT EDIT
# SUGARMAGIC WORKFLOW TEMPLATE VERSION: 13

name: SugarDeploy — wordlark v1

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref (branch / tag / sha) to deploy. Defaults to the workflow_dispatch ref.'
        required: false
        default: ''
      sugarmagic_ref:
        description: 'Sugarmagic engine ref to build target-web from. Defaults to main.'
        required: false
        default: ''

permissions:
  contents: read
  id-token: write

jobs:
  deploy-backend:
    name: Deploy backend to Cloud Run
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}
      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.SUGARMAGIC_WIF_PROVIDER }}
      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2
      - name: Configure docker for Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet
      - name: Run deploy.sh
        working-directory: deployment/google-cloud-run
        env:
          SUGARMAGIC_GCP_PROJECT_ID: wordlark-v1-1dqlc
          SUGARMAGIC_GCP_REGION: us-central1
          SUGARMAGIC_SERVICE_NAME_PREFIX: wordlark-v1-1dqlc
          SUGARMAGIC_RUNTIME_SA_EMAIL: ${{ vars.SUGARMAGIC_RUNTIME_SA_EMAIL }}
          SUGARMAGIC_ARTIFACT_REGISTRY_URL: us-central1-docker.pkg.dev/wordlark-v1-1dqlc/wordlark-v1-1dqlc
          SUGARMAGIC_ALLOWED_ORIGINS: "https://*--wordlark-prod.netlify.app,https://wordlark-prod.netlify.app,https://game.wordlarkhollow.com"
          SUGARMAGIC_SUGARAGENT_ANTHROPIC_MODEL: "claude-sonnet-4-5"
          SUGARMAGIC_SUGARAGENT_OPENAI_VECTOR_STORE_ID: "vs_69d16958aec48191b2fe258bd16bf028"
          SUGARMAGIC_SUGARAGENT_REGEN_MODEL: "claude-sonnet-4-5"
          SUGARMAGIC_SUGARAGENT_SUMMARY_MODEL: "claude-haiku-4-5"
          SUGARMAGIC_SUGARPROFILE_SUPABASE_ANON_KEY: "sb_publishable_XgdRpHfF_nDBdYLc4x1mTg_0CPfYcVZ"
          SUGARMAGIC_SUGARPROFILE_SUPABASE_URL: "https://fhhcmtbtozlxaboqhrla.supabase.co"
        run: bash deploy.sh

  deploy-frontend:
    name: Deploy frontend to Netlify
    runs-on: ubuntu-latest
    needs: deploy-backend
    steps:
      - name: Checkout game repo
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}
          fetch-depth: 0
      - name: Resolve game version
        id: game-version
        run: |
          GAME_VERSION=$(git describe --tags --always --dirty)
          echo "version=$GAME_VERSION" >> "$GITHUB_OUTPUT"
          echo "Game version: $GAME_VERSION"
      - name: Checkout sugarmagic engine
        uses: actions/checkout@v4
        with:
          repository: nikkileaps/sugarmagic
          ref: ${{ github.event.inputs.sugarmagic_ref || 'main' }}
          path: sugarmagic
          fetch-depth: 0
      - name: Verify boot.json exists
        run: |
          if [ ! -f .sugarmagic/published-web/boot.json ]; then
            echo "::error::.sugarmagic/published-web/boot.json missing — re-save the project in Studio and commit before re-dispatching."
            exit 1
          fi
      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.SUGARMAGIC_WIF_PROVIDER }}
      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2
      - name: Resolve Cloud Run gateway URL
        id: gateway
        run: |
          URL=$(gcloud run services describe wordlark-v1-1dqlc-sugarmagic-gateway --project wordlark-v1-1dqlc --region us-central1 --format 'value(status.url)')
          if [ -z "$URL" ]; then
            echo "::error::gcloud run services describe wordlark-v1-1dqlc-sugarmagic-gateway returned an empty URL. Is the gateway deployed? Run the backend job first."
            exit 1
          fi
          echo "url=$URL" >> "$GITHUB_OUTPUT"
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: sugarmagic/package.json
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: pnpm
          cache-dependency-path: sugarmagic/pnpm-lock.yaml
      - name: Install sugarmagic dependencies
        working-directory: sugarmagic
        run: pnpm install --frozen-lockfile
      - name: Build target-web engine bundle
        working-directory: sugarmagic
        env:
          NODE_ENV: production
          SUGARMAGIC_GAME_VERSION: ${{ steps.game-version.outputs.version }}
          VITE_SUGARMAGIC_GAME_MAJOR_VERSION: "1"
          VITE_SUGARMAGIC_VERSIONED_SLUG: "wordlark-v1-1dqlc"
          VITE_SUGARMAGIC_GIT_SHA: ${{ github.sha }}
          VITE_SUGARMAGIC_BUILD_TIMESTAMP: ${{ github.run_started_at }}
          VITE_SUGARMAGIC_GATEWAY_URL: ${{ steps.gateway.outputs.url }}
          VITE_SUGARMAGIC_SUGARAGENT_PROXY_BASE_URL: ${{ steps.gateway.outputs.url }}
          VITE_SUGARMAGIC_SUGARLANG_PROXY_BASE_URL: ${{ steps.gateway.outputs.url }}
        run: pnpm --filter @sugarmagic/target-web build
      - name: Stage built bundle and boot.json
        run: |
          rm -rf .sugarmagic/published-web/dist
          mkdir -p .sugarmagic/published-web/dist
          cp -R sugarmagic/targets/web/dist/. .sugarmagic/published-web/dist/
          cp .sugarmagic/published-web/boot.json .sugarmagic/published-web/dist/boot.json
          # boot.json is stamped at the END of this step, once the files
          # it points at are final — see 'Stamp each asset URL' below.
          # Cache rules + X-Game-Version for the deployed site. The
          # version is only knowable here (git describe on the runner),
          # so the managed file carries a placeholder we substitute.
          if [ -f .sugarmagic/published-web/_headers ]; then
            sed "s|__SUGARMAGIC_GAME_VERSION__|${{ steps.game-version.outputs.version }}|g" \
              .sugarmagic/published-web/_headers > .sugarmagic/published-web/dist/_headers
            if grep -q '__SUGARMAGIC_GAME_VERSION__' .sugarmagic/published-web/dist/_headers; then
              echo "::error::_headers still contains the version placeholder after substitution."
              exit 1
            fi
          fi
          # Ship exactly the files boot.json references. Its
          # assetSources keys ARE the paths, relative to the game root,
          # and each maps to the same path on the deployed site — so a
          # key copied to dist/<key> lands at the URL the game asks for,
          # whichever directory it came from (assets/, masks/, ...).
          #
          # Copying assets/ wholesale instead published every tracked
          # file whether referenced or not, and never shipped masks/ at
          # all, so every mask URL 404'd in production.
          #
          # Files are copied one at a time and nothing is deleted:
          # dist/assets/ also holds the Vite bundle output, which must
          # survive.
          # The list goes to a file rather than a pipe: `jq | while`
          # runs the loop in a subshell, where the missing-file flag
          # would be discarded and a broken game would deploy clean.
          jq -r '.assetSources | keys[]' \
            .sugarmagic/published-web/boot.json > /tmp/sugarmagic-assets.txt
          staged=0
          missing=0
          while IFS= read -r asset; do
            if [ -f "$asset" ]; then
              mkdir -p ".sugarmagic/published-web/dist/$(dirname "$asset")"
              cp "$asset" ".sugarmagic/published-web/dist/$asset"
              staged=$((staged + 1))
            else
              echo "::error::boot.json references a file that is not in the repo: $asset"
              missing=$((missing + 1))
            fi
          done < /tmp/sugarmagic-assets.txt
          echo "Staged $staged asset(s)."
          if [ "$missing" -ne 0 ]; then
            echo "::error::Refusing to deploy: $missing referenced asset(s) missing."
            exit 1
          fi
          # Compress the staged GLBs. Rewrites the staged copy only —
          # the project's own files are untouched, so Studio keeps
          # reading uncompressed originals. MUST come before the stamp
          # below, which hashes the bytes actually shipped.
          sugarmagic/node_modules/.bin/tsx \
            sugarmagic/scripts/compress-staged-glbs.ts \
            .sugarmagic/published-web/dist
          # Stamp each asset URL with a hash of its own bytes, so an
          # unchanged asset keeps its URL and stays cached across deploys.
          # This replaced a single per-deploy sha on every URL, which made
          # every deploy re-download every asset even when nothing changed.
          #
          # MUST run last: the hash has to cover the bytes actually shipped.
          # Anything that rewrites a staged file — compression, for one —
          # belongs ABOVE this, or the URL will not change when the file
          # does and players keep a stale asset for the year `_headers`
          # marks it immutable.
          while IFS= read -r asset; do
            printf '%s\t%s\n' "$asset" \
              "$(sha256sum ".sugarmagic/published-web/dist/$asset" | cut -c1-12)"
          done < /tmp/sugarmagic-assets.txt > /tmp/sugarmagic-hashes.tsv
          jq -Rn '[inputs | split("\t") | {key: .[0], value: .[1]}] | from_entries' \
            /tmp/sugarmagic-hashes.tsv > /tmp/sugarmagic-hashes.json
          jq --slurpfile h /tmp/sugarmagic-hashes.json \
            '.assetSources |= with_entries(.value += "?v=" + ($h[0][.key] // error("no content hash for " + .key)))' \
            .sugarmagic/published-web/dist/boot.json > .sugarmagic/published-web/dist/boot.json.tmp
          mv .sugarmagic/published-web/dist/boot.json.tmp .sugarmagic/published-web/dist/boot.json
      - name: Deploy to Netlify
        id: netlify
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: 2122920a-dbc2-443e-84ed-80743862033f
        run: |
          npx -y netlify-cli@17 deploy --site="$NETLIFY_SITE_ID" --dir=.sugarmagic/published-web/dist --message="SugarDeploy $GITHUB_REF_NAME" --json --prod > netlify-deploy.json
          cat netlify-deploy.json
          echo "url=$(jq -r '.url // .deploy_url // empty' netlify-deploy.json)" >> "$GITHUB_OUTPUT"
      # An upload that succeeds is not a deploy that landed. Ask the
      # live site which version it is serving and fail if it is not the
      # one we just built — catches a stale build staying live, a
      # no-op deploy, and a CDN that has not propagated.
      - name: Verify the deployed build
        env:
          EXPECTED: ${{ steps.game-version.outputs.version }}
          SITE_URL: ${{ steps.netlify.outputs.url }}
        run: |
          if [ -z "$SITE_URL" ]; then
            echo "::error::Netlify did not report a deploy URL; cannot verify."
            exit 1
          fi
          for attempt in 1 2 3 4 5; do
            SERVED=$(curl -sSI "$SITE_URL" | tr -d '\r' \
              | awk -F': ' 'tolower($1) == "x-game-version" { print $2 }')
            if [ "$SERVED" = "$EXPECTED" ]; then
              echo "Verified $SITE_URL is serving $SERVED."
              exit 0
            fi
            echo "Attempt $attempt: expected '$EXPECTED', got '${SERVED:-<no header>}'. Retrying..."
            sleep 5
          done
          echo "::error::$SITE_URL is serving '${SERVED:-<no header>}', expected '$EXPECTED'. The upload reported success but the site is not serving this build."
          exit 1
